@model IEnumerable<MVC_FUT_NFL.Models.Comentario>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Agregar">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Comentario1)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdPartidoNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Comentario1)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdPartidoNavigation.Id)
            </td>
            <td>
                    <a asp-action="Editar" asp-route-id="@item.Id">Edit</a> |
                    <a asp-action="Detalle" asp-route-id="@item.Id">Details</a> |
                    <a asp-action="Eliminar" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
