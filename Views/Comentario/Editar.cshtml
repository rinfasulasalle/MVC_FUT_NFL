@model MVC_FUT_NFL.Models.Comentario

@{
    ViewData["Title"] = "Editar";
}

<h1>Editar</h1>

<h4>Comentario</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Editar">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id" />
            <div class="form-group">
                <label asp-for="IdPartido" class="control-label"> Partido</label>
                <select asp-for="IdPartido" class="form-control" id="ddlIdPartido"
                        asp-items="@(new SelectList(ViewBag.IdPartido,"Id","Id"))"></select>
                <span asp-validation-for="IdPartido" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Comentario1" class="control-label">Comentario</label>
                <input asp-for="Comentario1" class="form-control" />
                <span asp-validation-for="Comentario1" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
